# Zagros Node — runtime dependencies.
#
# Deliberately small: the core drivers are vendored (vendor/zagros) and
# install their own binaries at runtime, so nothing core-specific is
# installed here.
fastapi>=0.115,<1
uvicorn[standard]>=0.30
pydantic>=2.9,<3
psutil>=5.9
cryptography>=43
pyOpenSSL>=24
grpcio>=1.60                 # sing-box per-user counters (StatsService gRPC)
protobuf>=4.25               # xray_api's generated stubs import google.protobuf
